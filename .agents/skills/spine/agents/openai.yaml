interface:
  display_name: "Spine"
  short_description: "Repo-specific patterns and workflows for spine"
  default_prompt: "Use the spine repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true